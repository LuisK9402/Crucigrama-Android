<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proyecto.cruzando">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DarkTheme">
        <activity android:name="com.proyecto.cruzando.WinActivity"></activity>
        <activity android:name="com.proyecto.cruzando.OptionsActivity" />
        <activity android:name="com.proyecto.cruzando.LevelSelectorActivity" />
        <activity android:name="com.proyecto.cruzando.CrosswordActivity" />
        <activity android:name="com.proyecto.cruzando.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>